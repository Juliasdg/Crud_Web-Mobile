export declare type Filter = (file: string) => boolean;
export declare type Config = {
    rootDirectory: string;
    recursively?: boolean;
    filter?: string | Filter;
};
export declare type LoadingResult = {
    content: any;
    name: string;
    path: string;
}[];
export declare function loadFiles(config: Config): Promise<LoadingResult>;
